import { makeStyles } from "@material-ui/core"

export const useStyles = makeStyles(()=> ({
   barContainerItem: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
   },
   resetContainer: {
      padding: '20px'
   }
      
}))